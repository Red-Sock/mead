services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: mead
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mead_db
    ports:
      - '15433:5432'
    volumes:
      - ./local_data/pg:/var/lib/postgresql
